describe('window.PANEL - Namespace da aplicacao', function () {

	it('Verifica se o objeto esta no formato esperado', function () {

		expect(window.PANEL).toEqual(jasmine.any(Object));

    });

    it('Teste 2', function () {
 
        expect(window.PANEL).toEqual(jasmine.any(Object));

	});


});